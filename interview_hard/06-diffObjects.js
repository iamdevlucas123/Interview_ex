/**
 * 06. Diff Objects
 *
 * Write a function that deeply compares two plain objects and returns
 * a list of differences.
 *
 * Output format:
 * [
 *   { path: "user.name", type: "changed", before: "Ana", after: "Anna" },
 *   { path: "user.age", type: "removed", before: 24, after: undefined },
 *   { path: "user.city", type: "added", before: undefined, after: "Lisbon" }
 * ]
 *
 * Notes:
 * - Handle nested objects and arrays
 * - You may assume there are no circular references
 */

function diffObjects(before, after) {
    // TODO: implement
}

