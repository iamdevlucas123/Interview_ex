/**
 * 10. Debounce
 *
 * Write a function debounce(fn, delay) that returns a new function.
 * The returned function should only call fn after it has stopped
 * being invoked for delay milliseconds.
 *
 * Example:
 * const log = debounce(() => console.log("run"), 300)
 * log()
 * log()
 * log()
 * // "run" should be printed only once, 300ms after the last call
 */

function debounce(fn, delay) {
    // TODO: implement
}
