/**
 * 09. LRU Cache
 *
 * Implement a small LRU (Least Recently Used) cache class.
 *
 * Requirements:
 * - constructor(limit): sets the maximum number of items
 * - get(key): returns the value if found, otherwise -1
 * - put(key, value): inserts or updates a value
 * - When the limit is exceeded, remove the least recently used item
 *
 * Example:
 * const cache = new LRUCache(2)
 * cache.put("a", 1)
 * cache.put("b", 2)
 * cache.get("a") -> 1
 * cache.put("c", 3) // removes "b"
 * cache.get("b") -> -1
 */

class LRUCache {
    constructor(limit) {
        this.limit = limit
        // TODO: initialize cache data
    }

    get(key) {
        // TODO: implement
    }

    put(key, value) {
        // TODO: implement
    }
}

