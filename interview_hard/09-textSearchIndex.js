/**
 * 09. Text Search Index
 *
 * Design a small in-memory search index.
 *
 * Requirements:
 * - add(id, text): stores a document
 * - search(term): returns all document ids containing the term
 * - remove(id): removes a document from the index
 *
 * Notes:
 * - Normalize by lowercasing and splitting on whitespace
 * - Avoid scanning every document on each search
 * - Consider update/removal consistency
 */

class TextSearchIndex {
    constructor() {
        // TODO: initialize internal state
    }

    add(id, text) {
        // TODO: implement
    }

    search(term) {
        // TODO: implement
    }

    remove(id) {
        // TODO: implement
    }
}

