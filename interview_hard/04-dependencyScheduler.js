/**
 * 04. Dependency Scheduler
 *
 * You receive a list of jobs and dependencies between them.
 * Return a valid execution order, or throw an error if a cycle exists.
 *
 * Example:
 * jobs = ["a", "b", "c", "d"]
 * dependencies = [
 *   ["a", "b"],
 *   ["a", "c"],
 *   ["b", "d"],
 *   ["c", "d"]
 * ]
 *
 * Meaning:
 * - "a" must run before "b"
 * - "a" must run before "c"
 * - "b" must run before "d"
 * - "c" must run before "d"
 */

function dependencyScheduler(jobs, dependencies) {
    // TODO: implement
}

