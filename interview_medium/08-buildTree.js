/**
 * 08. Build Tree From Flat List
 *
 * You receive a flat array of categories. Each item has:
 * { id: number, name: string, parentId: number | null }
 *
 * Write a function that returns a tree structure where each node has:
 * { id, name, children }
 *
 * Example:
 * [
 *   { id: 1, name: "Electronics", parentId: null },
 *   { id: 2, name: "Phones", parentId: 1 },
 *   { id: 3, name: "Laptops", parentId: 1 }
 * ]
 * ->
 * [
 *   {
 *     id: 1,
 *     name: "Electronics",
 *     children: [
 *       { id: 2, name: "Phones", children: [] },
 *       { id: 3, name: "Laptops", children: [] }
 *     ]
 *   }
 * ]
 */

function buildTree(items) {
    // TODO: implement
}

