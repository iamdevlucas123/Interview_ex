/**
 * 07. Frequency Sort
 *
 * Write a function that sorts an array by element frequency in descending order.
 * If two elements have the same frequency, keep the smaller value first.
 *
 * Examples:
 * frequencySort([4, 6, 2, 6, 4, 4, 1])
 * -> [4, 4, 4, 6, 6, 1, 2]
 */

function frequencySort(numbers) {
    // TODO: implement
}

